services:
  - type: web
    name: onmarket-backend
    env: python
    region: oregon
    buildCommand: cd backend && pip install -r requirements.txt
    startCommand: cd backend && python manage.py collectstatic --noinput && python manage.py migrate && gunicorn online_shop.wsgi:application --workers 2 --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
      - key: DJANGO_SETTINGS_MODULE
        value: online_shop.settings
      - key: WEB_CONCURRENCY
        value: "2"
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        sync: false
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "onmarket-3.onrender.com,onmarket-backend.onrender.com"
      - key: CORS_ALLOW_ALL_ORIGINS
        value: "False"
      - key: CORS_ALLOWED_ORIGINS
        value: "https://onmarket-frontend.onrender.com,https://onmarket.vercel.app,http://localhost:3000"
      - key: CSRF_TRUSTED_ORIGINS
        value: "https://onmarket-3.onrender.com,https://onmarket-frontend.onrender.com,https://onmarket.vercel.app,http://localhost:3000"

  - type: web
    name: onmarket-frontend
    env: static
    region: oregon
    buildCommand: cd frontend && npm install && npm run build
    staticPublish: frontend/build
    envVars:
      - key: REACT_APP_API_URL
        value: https://onmarket-3.onrender.com/api
      - key: NIX_SSL_CERT_FILE
        value: /etc/ssl/certs/ca-certificates.crt
